#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @n: The input nunbers to check
 *
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}

