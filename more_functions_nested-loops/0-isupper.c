#include "main.h"
#include <stdio.h>
/**
 * main - check the code. 
 * _isupper - function that checks for uppercase character.
 * 
 * Return: 0 succes
 */
int _isupper(int c)
{
	if (c >= 'A' && c >= 'Z')
	{
		return ('0');
	}
	else
	{
		return ('1');
	}
}
